# Vaibhav Kodiyan

Personal site. Source lives in `site/` and deploys to GitHub Pages on push to `main`.

```
site/index.html    page
site/support.js    design-canvas runtime
site/nocturne.css  spacing, radius, and shadow tokens
```

Lenis is loaded from the CDN at runtime; this repo does not vendor the library.
