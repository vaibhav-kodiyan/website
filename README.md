# Vaibhav Kodiyan

Personal site. Source lives in `site/index.html` and deploys to GitHub Pages on push to `main`.

The homepage is a self-contained, single-file scroll-timeline build — no external CSS/JS beyond Google Fonts and the audio track in `audio/`. Architecture notes live in `docs/plan.md`.
